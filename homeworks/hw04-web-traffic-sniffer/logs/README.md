# Local Validation Logs

This directory is reserved for local validation logs. Log files are ignored by
Git because they may contain machine paths, transient browser state, or raw
runtime details.

Tracked experiment summaries belong in `docs/experiment-log.md`.
