# Demo Runbook

## Purpose

Show that Sentinel can scan a small directory tree, detect one safe mock-virus fixture, keep clean files clean, separate heuristic warnings from confirmed signatures, and produce reproducible evidence.

Current checkpoint: `python3 demo/run_demo.py` passes `25` tests and regenerates the JSON report, Markdown report, pattern benchmark, and evidence manifest.

## Safety Rules

- Use only safe fixtures.
- Do not download, store, execute, or analyze live malware.
- Do not scan personal directories or third-party files.
- Do not delete, quarantine, upload, or modify scanned files.
- Keep symbolic links skipped by default.
- If literal EICAR is required, create it only in the final controlled demo environment.

## Demo Tree

```text
demo/demo-tree/
  clean/
    notes.txt
    image-placeholder.bin
  nested/
    level-1/
      level-2/
        sentinel-safe-mock-virus.txt
  suspicious/
    api-names-fixture.txt
  ignored-or-empty/
    empty.txt
```

| Path | Expected status | Reason |
| --- | --- | --- |
| `clean/notes.txt` | clean | Benign text fixture. |
| `clean/image-placeholder.bin` | clean | Binary-like fixture without a signature. |
| `nested/level-1/level-2/sentinel-safe-mock-virus.txt` | infected | MD5, SHA-256, and hex-pattern match. |
| `suspicious/api-names-fixture.txt` | suspicious | Heuristic-only API-name signal. |
| `ignored-or-empty/empty.txt` | clean | Empty file should not break scanning. |

## Run Sequence

1. State the safety boundary: read-only scanner, safe fixtures only, no live malware.
2. Show `signatures/malware-signatures.json`.
3. Show the demo tree.
4. Run `python3 demo/run_demo.py` or the manual scan command below.
5. Open `reports/demo-report.md`.
6. Point out the infected fixture, suspicious fixture, and clean files.
7. Point out scan metadata: Bloom-filter hash pre-check, exact hash-map verification, Aho-Corasick pattern engine, and `symlink_policy: skip`.
8. Open `reports/pattern-benchmark.md`.
9. Open `reports/demo-evidence-manifest.json`.
10. Show `python3 scripts/export_private_repo.py --dry-run` or the generated export manifest if the demo includes submission packaging.
11. State the final demo decision: the current final demo does not require literal EICAR; it uses a safe mock fixture plus EICAR reference hashes.
12. State the private-repo decision: the implementation was not moved to a private remote in this pass, by instruction; the curated export package is ready.
13. Close with the limitation: educational signature scanner, not production antivirus.

Manual scan command:

```bash
PYTHONPATH=src python3 -m sentinel scan demo/demo-tree \
  --signatures signatures/malware-signatures.json \
  --report reports/demo-report.json \
  --format json
```

Expected terminal summary:

```text
Sentinel scan complete: scanned=5 infected=1 suspicious=1 clean=3 errors=0 report=reports/demo-report.json
```

The scan returns exit code `1` because the safe mock-virus fixture is intentionally detected.

## Evidence Artifacts

| Artifact | Purpose |
| --- | --- |
| `reports/demo-report.json` | Machine-readable scan result. |
| `reports/demo-report.md` | Instructor-friendly scan result. |
| `reports/pattern-benchmark.json` | Synthetic Aho-Corasick benchmark evidence. |
| `reports/pattern-benchmark.md` | Readable benchmark summary. |
| `reports/demo-evidence-manifest.json` | SHA-256 hashes, safety flags, and reproduction commands. |
| `dist/sentinel-private-repo-manifest.json` | Local private-repo export manifest, generated by the export command. |
| `demo/demo-transcript.md` | Live-demo / video script basis. |

## Remaining Demo Work

- [x] Confirm whether literal EICAR is required. Current decision: no, unless the instructor explicitly overrides later.
- [ ] Run `python3 scripts/export_private_repo.py --clean` before private repo mirroring.
- [x] Record private repo URL and final commit hash. Private repo URL is not created/moved in this pass; final local demo commit is `2be51a0f003834e58795efcdd4b9224a730b90e7`.
- [ ] Re-run `python3 scripts/check_release.py` after private repo mirroring. Private repo mirroring is intentionally not performed in this pass.
- [x] Record a short demo video or finalize the live-demo script.
- [x] Run exported-package verification. No-install verification passed; editable install was blocked by missing `pip`.
