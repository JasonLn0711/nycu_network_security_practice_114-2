# Misc

Use this folder only when no better course folder fits.

Before adding a file here, check whether it belongs in `syllabus/`, `lectures/`, `handouts/`, `labs/`, `homeworks/`, `datasets/`, or `tools/`.
